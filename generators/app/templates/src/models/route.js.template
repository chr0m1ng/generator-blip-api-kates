class Route {
    constructor(path, method, controller, validator = null, middlewares = []) {
        this.path = path;
        this.method = method.toLowerCase();
        this.controller = controller;
        this.validator = validator;
        this.middlewares = middlewares;
    }
}

module.exports = Route;
