class Route {
    constructor(path, method, controller, validator = null) {
        this.path = path;
        this.method = method.toLowerCase();
        this.controller = controller;
        this.validator = validator;
    }
}

module.exports = Route;
